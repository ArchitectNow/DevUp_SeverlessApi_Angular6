export const environment = {
  production: true,
  sessionTimeout: 30,
  appConfig: {
    authenticationAuthority: 'https://<prod-auth-url>',
    baseUrl: 'https://<prod-url>',
    apiBaseUrl: 'https://<prod-url>/api'
  }
};
